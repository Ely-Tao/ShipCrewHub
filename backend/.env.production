# 🚀 Railway 生产环境配置

# 应用配置
NODE_ENV=production
PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-railway

# 数据库配置
# Railway 会自动注入以下变量之一：
# DATABASE_URL=mysql://user:password@host:port/database (MySQL)
# MYSQL_URL=mysql://user:password@host:port/database (MySQL 替代)
# POSTGRESQL_URL=postgresql://user:password@host:port/database (PostgreSQL)

# Redis 配置（如果添加了 Redis 服务）
# REDIS_URL=redis://user:password@host:port (Railway 自动提供)

# API 配置
API_VERSION=v1
LOG_LEVEL=info

# 跨域配置 - 替换为你的 Vercel 前端域名
CORS_ORIGIN=https://your-frontend-domain.vercel.app

# 文件上传配置
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# 生产环境优化
NODE_OPTIONS=--max-old-space-size=512

